on:
  push:
    branches:
      - github_test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: run tests
        uses: ./.github/actions/run_tests
